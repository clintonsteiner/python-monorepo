"""BUILD file for hildie-archive-git-forks tests."""

load("//tools:pytest.bzl", "pytest_tests")

pytest_tests(
    name = "tests",
    srcs = glob(["tests/**/test_*.py"]),
    deps = [
        "//:hildie",
        "@pip//click",
    ],
)
